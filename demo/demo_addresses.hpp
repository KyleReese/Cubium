#ifndef DEMO_ADDRESSES_HPP
#define DEMO_ADDRESSES_HPP

const LogicalAddress la_LSM(1,0); // local subnet manager
const LogicalAddress la_CA(1,1); // component A
const LogicalAddress la_CB(1,2); // component B

#endif
